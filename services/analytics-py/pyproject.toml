[project]
name = "analytics-py"
version = "0.1.0"
description = "FHIR eCQM analytics service — NATS consumer, measure compute, TimescaleDB writer"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
]

[tool.uv]
package = false
